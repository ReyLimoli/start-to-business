<div class="container">
  <div class="row">
    <div class="col-sm-6 mt-4 mb-4">
      <h2 class="mb-4">Investidores</h2>
    </div>
  </div>
</div>

<div class="container">
    <div class="row">
        <% @investors.each do |investor| %>
            <div class="col-sm-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"><%= investor.name %></h5>
                    </div>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item"><strong>e-mail: </strong><%= investor.email %></li>
                        <li class="list-group-item"><strong>Valor disponível para investir: </strong> R$ <%= investor.amount_available_to_invest %> </li>
                    </ul>
                    <div class="card-body">
                        <a href="#" class="card-link">Enviar mensagem</a>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</div>