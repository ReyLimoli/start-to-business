<div class="mt-4 mb-4">
<div class="margin jumbotron jumbotron-fluid">
    <div class='container'>
        <div class="row">
            <div class="col-sm-8">
                <h1 class="display-4">Bem-vindo ao S2Business</h1>
                <p class="lead">Onde você pode investir em uma ideia e criar a sua!</p>
            </div>
        </div>
    </div>
</div>
</div>

<div class="bg mt-0">
</div>

<% if user_signed_in? %>
  <% @ideas.each do |idea|%>
  <div id="idea-<%=idea.id%>">
    <h1><%= idea.title %></h1>
    <p><%= truncate(idea.description, length: 25, omission: '...') { link_to 'Ver detalhes', idea_path(idea.id) } %></p>
    <p><%= number_to_currency(idea.initial_investment_value, unit: 'R$ ', separator:',', delimiter: '' ) %></p>
    <p><%= idea.estimated_project_time_to_month %></p>
    <p><%= idea.estimated_time_to_profit_to_month %></p>
  </div>
  <% end%>
<% end %>

